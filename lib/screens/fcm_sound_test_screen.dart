import 'package:flutter/material.dart';

class FCMSoundTestScreen extends StatelessWidget {
  const FCMSoundTestScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('FCM Sound Test'),
      ),
      body: const Center(
        child: Text('FCM Sound Test Screen - Not implemented'),
      ),
    );
  }
}
